<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="barryalan.ediary70">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".welcomePage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".login" />
        <activity android:name=".registration" />
        <activity android:name=".forgotLogin" />
        <activity android:name=".profilePage" />
        <activity android:name=".goalsMenu" />
        <activity android:name=".healthCareMenu" />
        <activity android:name=".shortTermGoals" />
        <activity android:name=".longTermGoals" />
        <activity android:name=".contactsPage" />
        <activity android:name=".settingsPage" />
        <activity android:name=".newGoal"></activity>
    </application>

</manifest>